# Индекс промптов

Все промпты проекта в одном месте.

## Требования

### Phase A — Первоначальное формирование требований

```markdown
Переведи следующие требования в формат TOGAF Phase A (Architecture Vision). Структура: цель и ценность, stakeholders, scope, принципы, high‑level требования. Сохрани смысл, добавь недостающие разделы по шаблону Phase A.
```

### Phase A — Уточнения требований

```markdown
Вот документ Architecture Vision (Phase A). Задай один уточняющий вопрос, который поможет сделать документ более полным и однозначным. После моего ответа обнови документ и задай следующий вопрос. Повторяй, пока не останется неясностей. Сначала — вопрос.
```

### Phase B|C|D — Первоначальное формирование требований

```markdown
Вот документы по фазам [A, B, C — перечисли заполненные]. Добавь документацию по следующей фазе (B, C или D) согласно TOGAF ADM. Опирайся на все предыдущие артефакты как контекст. Сохрани согласованность.
```

### Phase B|C|D — Уточнения требований

```markdown
Вот документ по фазе [B / C / D]. Задай один уточняющий вопрос, который поможет сделать документ более полным и однозначным. После моего ответа обнови документ и задай следующий вопрос. Повторяй, пока не останется неясностей. Сначала — вопрос.
```

---

## Реализация

### Phase E|F — Первоначальный план миграции

```markdown
Вот документы по фазам A, B, C, D. Добавь Migration Plan (фазы E–F TOGAF): варианты решения, building blocks, последовательность изменений, итерации. Опирайся на все предыдущие артефакты. Сохрани согласованность.
```

### Phase E|F — Уточнения плана миграции

```markdown
Вот Migration Plan. Задай один уточняющий вопрос, который поможет сделать план более полным и однозначным. После моего ответа обнови план и задай следующий вопрос. Повторяй, пока не останется неясностей. Сначала — вопрос.
```

### Phase E|F — Разбиваем Migration Plan на Work Packages

```markdown
Вот Migration Plan. Разбей его на небольшие Work Packages. Каждый WP: входные артефакты, ожидаемый результат, критерии проверки. WP должен быть маленьким — выполнимым за 1–2 часа. Задай один уточняющий вопрос, если нужно. После моего ответа обнови разбиение и задай следующий вопрос. Повторяй, пока разбиение не станет полным. Сначала — разбиение или вопрос.
```

### Реализация — Следование Work Packages

```markdown
Вот список Work Packages. Реализуй первый WP. После завершения покажи, что сделано, и спроси: приступать ли к следующему WP? Не переходи к следующему без моего подтверждения. Жди проверки перед каждым следующим WP.
```

---

## Вспомогательные

### UI UX Pro Max — Установка

```markdown
Установи https://github.com/nextlevelbuilder/ui-ux-pro-max-skill
```

### UI UX Pro Max — Применение

```markdown
Примени UI UX Pro Max к проекту @src/frontend.

Цель: обновить UI/UX до современного, консистентного, production-ready уровня без изменения бизнес-логики.

Сделай по шагам:
1) Проанализируй текущий фронтенд в @src/frontend (структура, дизайн-система, компоненты, стили, доступность, адаптивность).
2) Сгенерируй рекомендуемую дизайн-систему (цвета, типографика, spacing, states, компоненты, паттерны страниц) под тип продукта meetup reservation.
3) Дай короткий план работ с приоритетами (quick wins -> medium -> advanced).
4) Реализуй изменения итеративно прямо в коде @src/frontend:
   - сначала токены/базовые стили
   - затем общие UI-компоненты
   - затем ключевые экраны
5) Обеспечь:
   - a11y (focus states, contrast, keyboard nav)
   - responsive (mobile/tablet/desktop)
   - единый стиль кнопок, форм, карточек, модалок
   - аккуратные hover/transition состояния
6) После каждого этапа показывай:
   - что изменено
   - какие файлы затронуты
   - как проверить результат локально

Ограничения:
- Не ломай существующую логику и API-контракты.
- Не вноси изменения вне @src/frontend без явной необходимости.
- Если чего-то не хватает (например, стек/библиотека UI), сначала спроси уточнение.
```

---

## Spec Kit (функция `specs/001-qr-checkin/`)

### Конституция

```markdown
/speckit.constitution Собери конституцию проекта из spec/phase-a-architecture-vision.md (принципы AP-1–AP-5, границы раздела 3.2) и spec/phase-d-technology-architecture.md (стандарты TS-01–TS-18). Пять принципов, ограничения, порядок работы, правила отступлений.
```

### Спецификация функции

```markdown
/speckit.specify Чек-ин по QR-коду. Сейчас организатор отмечает участников вручную (PATCH /registrations/{id}/check-in). Нужно: уникальный QR в письме-подтверждении, страница сканирования у организатора, повторное сканирование отклоняется, ручная отметка остаётся. Источник: FR-05.5 в spec/phase-b-business-architecture.md.
```

### Уточнения по одному

```markdown
/speckit.clarify
```

### План, задачи, проверка

```markdown
/speckit.plan Стек и ограничения — из конституции. Схема БД только SQL-скриптом в src/backend/db/. QR генерировать внутри backend, внешние сервисы не использовать. Новые библиотеки — с обоснованием в Complexity Tracking.
```

```markdown
/speckit.tasks
```

```markdown
/speckit.analyze
```

### Реализация и сверка

```markdown
/speckit.implement Выполняй задачи по одной. После каждой — dotnet build и npm run build. Не переходи к следующей без моего подтверждения.
```

```markdown
/speckit.converge
```

---

## OpenSpec (`openspec/`)

### Описание текущего состояния

```markdown
/opsx:onboard Опиши текущее состояние системы в openspec/specs/: одна папка на бизнес-возможность BC-01–BC-10 из spec/phase-b-business-architecture.md. Требования — из FR-01–FR-09 и реального поведения контроллеров в src/backend/Controllers. Каждое требование через SHALL и хотя бы один сценарий WHEN/THEN. Ничего не придумывай сверх того, что есть в коде.
```

### Изменение по шагам

```markdown
/opsx:new waitlist
```

```markdown
/opsx:continue
```

Повторять `/opsx:continue`, пока не созданы proposal, дельты спецификаций, design и tasks. Перед `tasks` — проверка: текущее состояние ↔ proposal ↔ design ↔ дельты.

### Быстрый путь

```markdown
/opsx:propose waitlist Лист ожидания на бесплатные типы билетов: при исчерпании мест участник встаёт в очередь, при освобождении места первый в очереди автоматически переводится в регистрацию и получает письмо. Платные билеты — вне границ до внедрения платежей.
```

### Реализация, проверка, синхронизация

```markdown
/opsx:apply waitlist
```

```markdown
/opsx:verify waitlist
```

```markdown
/opsx:sync waitlist
```

```markdown
/opsx:archive waitlist
```
